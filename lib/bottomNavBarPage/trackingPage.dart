import 'package:flutter/material.dart';

class trackingPage extends StatelessWidget {
  const trackingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ini adalah halaman tracking'),
    );
  }
}
