import 'package:flutter/material.dart';

class petCheckerPage extends StatelessWidget {
  const petCheckerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ini adalah halaman pet checker'),
    );
  }
}
